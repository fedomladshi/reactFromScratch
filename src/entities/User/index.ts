import { UserSchema, User } from './model/types/user';
import {
    userSlice,
    userReducer,
    userActions,
} from './model/slice/userSlice';

export {
    userSlice,
    userReducer,
    userActions,
    UserSchema,
    User,
};
